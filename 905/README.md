# Erdős Problem 905

> Every graph with `n` vertices and more than `n^2 / 4` edges contains an edge
> lying in at least `n / 6` triangles.

Primary reference: <https://www.erdosproblems.com/905>

The Lean formalization lives in `../ErdosProblems/P905.lean`.
